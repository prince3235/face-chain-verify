fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.8.2
pydantic-settings==2.4.0
numpy==1.26.4
opencv-python-headless==4.10.0.84
Pillow==10.4.0
web3==6.20.1
python-dotenv==1.0.1

# Optional — production-grade face embeddings instead of the OpenCV fallback.
# Requires cmake + a C++ build toolchain to compile dlib. Uncomment once your
# environment has those available:
# face_recognition==1.3.0
# dlib==19.24.6

# Dev/test only
pytest==8.3.2
httpx==0.27.0
