# URL of the deployed FastAPI backend (face + search + chain services)
BACKEND_URL=http://localhost:8000

# Set to "true" to run the UI against mocked pipeline responses
# when no backend is running yet (useful for frontend-only dev/demo)
NEXT_PUBLIC_USE_MOCK_PIPELINE=true
